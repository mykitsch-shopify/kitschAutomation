{
  "name": "kitsch-automation",
  "description": "QA harness for mykitsch.com. Heroku CI runs the whole offline tier — static gates, fixture-driven browser suites, and every detection control. The live-store tiers stay on GitHub Actions, which holds the credentials. See docs/HEROKU-CI.md.",
  "repository": "https://github.com/mykitsch-shopify/kitschAutomation",
  "keywords": ["qa", "playwright", "shopify", "i18n", "accessibility"],
  "stack": "heroku-24",
  "environments": {
    "test": {
      "buildpacks": [{ "url": "heroku-community/apt" }, { "url": "heroku/nodejs" }],
      "env": {
        "NODE_ENV": "test",
        "NPM_CONFIG_PRODUCTION": "false",
        "KITSCH_BASE_URL": "http://127.0.0.1:4173",
        "PLAYWRIGHT_BROWSERS_PATH": "0",
        "CI": "true"
      },
      "scripts": {
        "test-setup": "npx playwright install chromium",
        "test": "npm run ci:offline"
      }
    }
  }
}
